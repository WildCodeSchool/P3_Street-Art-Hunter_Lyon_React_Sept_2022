import React from "react";

export default function Allive() {
  // const [isActive, setIsActive] = useState("");
  return (
    <div className="p-1 flex justify-evenly">
      {/* {isActive === "scores" ? (
      <button
        type="submit"
        className="  from-lightblue bg-gradient-to-br to-pink w-1/3 font-main-font text-2xl group relative flex justify-center rounded-full border py-2 border-indigo-500/50 font-medium text-lightblue hover:bg-indigo-500/50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
      >
        <span className=" absolute inset-y-0 left-0 flex items-center pl-3" />
        ALL
      </button>
        ) : (
      <button
        type="submit"
        className=" w-1/3 font-main-font text-2xl group relative flex justify-center rounded-full border py-2 border-indigo-500/50 font-medium text-lightblue hover:bg-indigo-500/50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
      >
        <span className=" absolute inset-y-0 left-0 flex items-center pl-3" />
        LIVE
      </button> */}
    </div>
  );
}
